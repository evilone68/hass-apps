Configuration
=============

You first need to specify the correct actor type at the top level of
Schedy's configuration:

::

    actor_type: thermostat

Then, go on and add thermostats as actors to your rooms.

.. include:: ../config_intro

.. literalinclude:: config.yaml
   :language: yaml
